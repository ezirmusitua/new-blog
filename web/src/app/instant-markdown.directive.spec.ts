import { InstantMarkdownDirective } from './instant-markdown.directive';

describe('InstantMarkdownDirective', () => {
  it('should create an instance', () => {
    const directive = new InstantMarkdownDirective();
    expect(directive).toBeTruthy();
  });
});
